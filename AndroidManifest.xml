<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iodice.rssreader"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Supported Versions -->
    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19" />
    
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Application -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <activity
            android:name="com.iodice.rssreader.Activity_Home"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
                
        <activity
            android:name="com.iodice.rssreader.Activity_Rss"
            android:label="@string/app_name"
            android:parentActivityName="com.iodice.rssreader.Activity_Home" >
            
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.iodice.rssreader.Activity_Main" />
        </activity>
        
    </application>

</manifest>
